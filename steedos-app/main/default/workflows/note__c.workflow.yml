rules:
  - name: finish
    active: true
    formula: label eq '已完成'
    label: 完成
    object_name: note__c
    trigger_type: onCreateOrTriggeringUpdate
    updates_field_actions:
      - wZZ8H5CShBTyLTRbd
fieldUpdates:
  - name: update_label
    field_name: label__c
    formula: label eq '待完成'
    label: 修改标签
    object_name: note__c
    operation: formula
    target_object: note__c
